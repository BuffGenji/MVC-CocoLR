{
    "name": "testing/slim_project",
    "description": "This is going to be a try of the application using slim, and most  likely some other language (Go) as a backend later on if possible",
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "authors": [
        {
            "name": "Ryan Heuvel"
        }
    ],
    "require": {
        "slim/slim": "^4.14",
        "slim/psr7": "^1.7",
        "symfony/var-dumper": "^7.2",
        "slim/twig-view": "^3.4",
        "pimple/pimple": "^3.5",
        "php-di/php-di": "^7.0",
        "slim/php-view": "^3.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.5"
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml"
    }
}
